---
import { Image } from "astro:assets";
import Layout from "@/layouts/Layout.astro";
import ClippedImage from "@/components/ClippedImage.astro";
import SvgIcon from "@/components/SvgIcon.astro";
import logo from "./img/_logo.webp";
import lakeYamanaka from "./img/_lake-yamanaka.webp";
import minami1 from "./img/_minami-1.webp";

const linkTags = [
  {
    rel: "preconnect",
    href: "https://fonts.googleapis.com",
  },
  {
    rel: "preconnect",
    href: "https://fonts.gstatic.com",
    crossorigin: "",
  },
  {
    rel: "stylesheet",
    href: "https://fonts.googleapis.com/css2?family=Yuji+Syuku&display=swap&text=テーマ『起きよ！輝け』イザヤ60:1",
  },
];
---

<Layout
  title="2025 第56回JBBF全国青年フェローシップキャンプ"
  favicon={logo.src}
  linkTags={linkTags}
>
  <header class="block">
    <h1 class="header-head">
      <small>2025 第56回</small><br />
      JBBF全国青年フェローシップキャンプ
    </h1>
  </header>

  <main class="main">
    <section class="block theme">
      <div>
        <h2 class="theme-head">テーマ</h2>
        <p class="theme-text">『起きよ！輝け！』</p>
        <div class="theme-verse">イザヤ 60:1</div>
      </div>
    </section>

    <section class="animated-image">
      <ClippedImage src={lakeYamanaka} alt="" />
    </section>

    <section class="box">
      <dl class="info-dl">
        <dt class="info-dt">
          <span class="info-icon">
            <SvgIcon type="calender" />
          </span>
          Date
        </dt>

        <dd class="info-dd">2025.05.05 - 2025.05.07</dd>

        <dt class="info-dt">
          <span class="info-icon">
            <SvgIcon type="location" />
          </span>
          Location
        </dt>

        <dd class="info-dd">
          グリーンヒルズニューみなみ<br />
          GREEN HILL'S NEW MINAMI
        </dd>
        <dd class="info-dd">
          <small>〒401-0502</small><br />
          山梨県南都留郡山中湖村平野520-45
        </dd>
      </dl>
    </section>

    <!-- <section>
      <ul class="link-list">
        <li>
          <a href="#" class="link-item">
            <Image src={minami1} alt="" class="link-image" />
          </a>
        </li>
      </ul>
    </section> -->
  </main>

  <footer class="footer">
    <small>
      主催：仁戸名聖書バプテスト教会・幕張聖書バプテスト教会<br />
      協力：市原聖書バプテスト教会
    </small>
  </footer>
</Layout>

<style>
  body {
    background: black;
    transition: background-color 1s;
  }

  body.light {
    background: white;
    transition: background-color 2s linear;
  }

  .block {
    align-items: center;
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 100vh;
  }

  .box {
    align-items: center;
    display: flex;
    justify-content: center;
  }

  .header-head {
    color: gray;
    font-size: 2vw;
    margin: 0;
  }

  .main {
    margin: auto;
    max-width: 600px;
    padding: 1em;
  }

  .footer {
    font-size: 90%;
    padding: 25vh 1em 10vh;
    text-align: center;
  }

  .theme {
    color: black;
    font-family: "Yuji Syuku", serif;
    font-size: 3vw;
    min-height: 50vh;
    padding: 0 0 40vh;
    text-shadow: 0 0 40px white;
  }

  .theme-head {
    font-size: 50%;
    font-weight: normal;
    margin: 0;
  }

  .theme-text {
    margin: 8px 0;
  }

  .theme-verse {
    font-size: 50%;
    text-align: right;
  }

  .info-dl {
    margin: 10vh 0;
  }

  .info-dt {
    color: var(--color-primary);
    font-size: 1.25em;
    font-weight: normal;
    margin: 2em 0 0;
  }

  .info-icon {
    display: inline-block;
    height: 1em;
    vertical-align: -0.15em;
    width: 1em;
  }

  .info-dd {
    margin: 0.5em 0;
  }

  .link-list {
    list-style: none;
    margin: 10vh auto;
    padding: 1em;
    max-width: 400px;
  }

  .link-item {
    display: block;
    margin: 1em 0;
  }

  .link-image {
    border-radius: 4px;
    height: 120px;
    object-fit: cover;
  }

  @media (max-width: 768px) {
    .header-head {
      font-size: 18px;
    }

    .theme {
      font-size: 32px;
    }
  }

  @keyframes filter-change {
    0% {
      filter: none;
    }
    25% {
      filter: brightness(125%);
    }
    50% {
      filter: none;
    }
    75% {
      filter: sepia(25%);
    }
    100% {
      filter: none;
    }
  }

  .animated-image {
    animation: filter-change 12s infinite;
  }
</style>

<script>
  const scrollTarget = document.querySelector(".main");
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting && entry.target instanceof HTMLElement) {
          document.body.classList.add("light");
        } else {
          document.body.classList.remove("light");
        }
      });
    },
    { rootMargin: "0px 0px -50%" }
  );

  if (scrollTarget) {
    observer.observe(scrollTarget);
  }
</script>
